{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "docker:composer": "docker compose run --rm builder composer install",
        "docker:npm": "docker compose run --rm builder npm ci",
        "docker:build": "docker compose run --rm builder npm run build",
        "docker:dev": "docker compose run --rm --service-ports builder npm run build",
        "docker:watch": "docker compose run --rm --service-ports builder npm run dev"
    },
    "devDependencies": {
        "bootstrap-sass": "^3.4.3",
        "jquery": "^3.7.0",
        "laravel-vite-plugin": "^0.8.0",
        "lodash": "^4.17.21",
        "sass": "^1.56.0",
        "vite": "^4.0.0"
    }
}
